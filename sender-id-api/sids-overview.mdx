---
title: "Sender IDs overview"
description: "Register and manage alphanumeric Sender IDs across global markets via API."
---

## What is a Sender ID?

A Sender ID (SID) is the alphanumeric name displayed as the sender when an SMS arrives on a recipient's handset. For example, `TrackFast` instead of a random phone number.

In markets with mandatory SID pre-registration (such as Australia under the ACMA SMS Sender ID Register), unregistered SIDs are replaced with `Unverified`. Registering your SIDs ensures your brand appears as intended.

## SID lifecycle

```
pending_review → pending_operator_review → approved → pending_register → pending_owner_confirmation → registered
              └→ rejected               └→ info_requested (resubmit)                                └→ suspended
                                         └→ rejected                                                  └→ deregistered
```

| Status | Description |
|---|---|
| `pending_review` | Submitted, awaiting Senderz validation against market rules. |
| `pending_operator_review` | Validated by Senderz, sent to the relevant network operator for review. |
| `info_requested` | Operator has requested additional information. Respond via `PATCH /v1/sids/{id}` with the requested fields. |
| `approved` | Operator has approved the registration. Proceeds to market register submission. |
| `pending_register` | Approved, submitted to market register (e.g. ACMA). |
| `pending_owner_confirmation` | Awaiting brand owner confirmation (e.g. ACMA Assist/myID step for AU). |
| `registered` | Live on the register. Messages display the branded SID. |
| `rejected` | Registration denied by Senderz validation or by the operator. See `rejection_reason`. |
| `suspended` | Temporarily suspended by register or Senderz. |
| `deregistered` | Removed from register. |

<Info>
  Not all markets include an operator review step. In regulator-led schemes (e.g. AU/ACMA), registrations may proceed directly from `pending_review` to `pending_register`. The `pending_operator_review` and `info_requested` statuses appear in markets with industry-led or operator-led registration schemes.
</Info>

## SID rules

| Rule | Value |
|---|---|
| Length | 2-11 characters |
| Characters | Alphanumeric (A-Z, a-z, 0-9) and spaces |
| Restriction | Cannot be numbers only |
| Match requirement | Must match the brand's registered business name, company name, trademark, or domain |

## Market-specific validation

Validation rules are applied server-side based on the `market` field. For example, the AU market applies ACMA restricted-word checks and ABR cross-referencing. A `422` response with a market-specific error code is returned if rules are violated.

New markets are added by extending the market configuration. No API version change is required. Customers integrating against `/v1/` pick up new markets by passing the relevant country code.
